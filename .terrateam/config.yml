# Terrateam configuration - auto-generated by DeployFlow
# Documentation: https://terrateam.io/docs/configuration

workflows:
  - tag_query: ""
    plan:
      - type: env
        name: ARM_CLIENT_ID
        cmd: ['echo', '${{ secrets.ARM_CLIENT_ID }}']
      - type: env
        name: ARM_CLIENT_SECRET
        cmd: ['echo', '${{ secrets.ARM_CLIENT_SECRET }}']
      - type: env
        name: ARM_SUBSCRIPTION_ID
        cmd: ['echo', '${{ secrets.ARM_SUBSCRIPTION_ID }}']
      - type: env
        name: ARM_TENANT_ID
        cmd: ['echo', '${{ secrets.ARM_TENANT_ID }}']
      - type: init
      - type: plan
    apply:
      - type: env
        name: ARM_CLIENT_ID
        cmd: ['echo', '${{ secrets.ARM_CLIENT_ID }}']
      - type: env
        name: ARM_CLIENT_SECRET
        cmd: ['echo', '${{ secrets.ARM_CLIENT_SECRET }}']
      - type: env
        name: ARM_SUBSCRIPTION_ID
        cmd: ['echo', '${{ secrets.ARM_SUBSCRIPTION_ID }}']
      - type: env
        name: ARM_TENANT_ID
        cmd: ['echo', '${{ secrets.ARM_TENANT_ID }}']
      - type: init
      - type: apply

when_modified:
  file_patterns:
    - "**/*.tf"
    - "**/*.tfvars"
  autoplan: true
  autoapply: false

dirs:
  .:
    tags:
      - deploy
      - dev
      - azure
